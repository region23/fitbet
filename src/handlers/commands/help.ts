import type { BotContext } from "../../types";

export async function helpCommand(ctx: BotContext) {
  await ctx.reply(
    `📖 *Справка по FitBet*\n\n` +
      `*Основные команды:*\n` +
      `/start — начать работу с ботом\n` +
      `/create — создать челлендж (в группе)\n` +
      `/status — статус текущего челленджа\n` +
      `/help — эта справка\n\n` +
      `*Как работает FitBet:*\n\n` +
      `1️⃣ *Создание челленджа*\n` +
      `Администратор группы создаёт челлендж командой /create. ` +
      `Настраиваются длительность, ставка и правила.\n\n` +
      `2️⃣ *Присоединение*\n` +
      `Участники нажимают кнопку "Участвовать" и проходят онбординг ` +
      `в личке бота: выбирают трек (Cut/Bulk), вводят метрики, ` +
      `загружают фото и ставят цели.\n\n` +
      `3️⃣ *Оплата*\n` +
      `После онбординга участники оплачивают ставку. ` +
      `Bank Holder подтверждает получение оплаты.\n\n` +
      `4️⃣ *Чек-ины*\n` +
      `Каждые 2 недели открывается окно чек-ина на 48 часов. ` +
      `Нужно отправить вес, талию и 2 фото.\n\n` +
      `5️⃣ *Финал*\n` +
      `По окончании челленджа автоматически рассчитываются победители. ` +
      `Формула: 70% достижение цели + 30% дисциплина.\n\n` +
      `*Треки:*\n` +
      `🔥 *Cut* — похудение (снижение веса и талии)\n` +
      `💪 *Bulk* — набор массы (увеличение веса)\n\n` +
      `Вопросы? Напишите в поддержку.`,
    { parse_mode: "Markdown" }
  );
}
